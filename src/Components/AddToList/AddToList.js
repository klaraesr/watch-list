import React, {Component} from 'react';

// Located at bottom of ProfilePage

// Conditional rendering! Either show two buttons for the user to add it to any of their lists. Otherwise, display text
// about that it is included in their list.
class AddToList extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

export default AddToList;
